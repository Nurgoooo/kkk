[build]
  command = "CI= npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# Этот блок полностью отключает автоматический плагин Next.js
[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
    disable = true